# TODO - Tahap 1 (Login Instagram + Profile)

- [ ] Tambahkan fitur backend endpoint:
  - [ ] GET /api/instagram/login (redirect Meta OAuth)
  - [ ] GET /api/instagram/callback (tukar code->token, simpan DB)
  - [ ] GET /api/instagram/profile (ambil data akun: id, username, profile_picture_url, followers_count, following_count, media_count)
- [ ] Perluas `server/services/instagram.service.js` untuk fetch profile picture & counts + mapping error.
- [ ] Tambahkan route mounting di `server/app.js` (atau buat router baru dan mount).
- [ ] Frontend: update UI Instagram connection (tombol connect + tampil profil dari /api/instagram/profile) agar sesuai “login berhasil dulu”.
  - [ ] Pastikan setelah login sukses, data profil tampil tanpa fitur engagement.
  - [ ] Implementasi tombol Disconnect (menggunakan mechanism yang ada; jika belum ada, tambahkan minimal endpoint disconnect).
- [ ] Testing:
  - [ ] Setup env Meta Developer App + redirect URI
  - [ ] Jalankan server & frontend di localhost
  - [ ] Flow: Connect -> OAuth login -> callback -> profil tampil
